# JXSocialFleet 项目目录规范

## 产品命名规范

- 软件正式对外显示名称统一为“极星AI社媒”。
- 菜单、窗口标题、用户提示、设计稿和后续宣传物料必须使用“极星AI社媒”，不得继续将 `JXSocialFleet` 作为用户可见产品名。
- `JXSocialFleet` 仅作为现有工程名、文件名、代码命名空间或兼容标识保留；未经明确迁移计划，不得批量重命名工程路径、配置键或本地存储键。

## Logo 资源规范

- 当前项目确认使用的主 Logo 高清母版为 `03-UI-Design/assets/jxsocialfleet-logo.svg`。
- 该 SVG 是无损矢量母版，后续菜单图标、应用图标、宣传图和切图必须优先从该文件导出，不要重复绘制或使用截图作为母版。
- Logo 的蓝色背景、安卓绿色主体、社交媒体聊天气泡/播放按钮和 AI 星芒属于已确认的品牌识别元素；后续修改页面时不得擅自替换、拉伸、裁剪或改变比例。
- 菜单左上角使用 `assets/jxsocialfleet-logo.svg`，显示尺寸由页面样式控制；需要新增 PNG/WebP 等位图切图时，应保持 SVG 母版不变，并按实际用途导出对应尺寸。

## 目录职责

- `01-Project`：项目代码目录。所有工程文件、源代码、配置文件、测试代码及代码相关资源统一存放在此目录中；编写或修改代码时必须在此目录内进行。
- `02-Product-Requirements`：产品需求说明文档目录。所有产品需求、业务规则、功能说明、流程说明及需求变更记录统一存放在此目录中。
- `03-UI-Design`：UI 设计目录。所有界面设计稿、交互设计、视觉规范、设计资源及 UI 设计说明统一存放在此目录中；涉及设计的产出必须存放在此目录内。

## 执行要求

- 不得将代码、产品需求文档或 UI 设计文件混放到其他职责目录。
- 新增内容前，先根据内容类型确认目标目录，并保持现有目录结构清晰。
- 除非用户明确要求，不在项目根目录直接创建工程文件、源代码、需求文档或 UI 设计文件。

## 下拉控件统一样式规范

项目内所有下拉控件必须保持统一的视觉风格和交互行为。新增页面、弹窗、表单或筛选区域时，禁止直接使用浏览器/WPF 原生下拉框的默认外观，也不得为单个页面重新设计一套风格不同的下拉控件。

### 视觉风格

- 使用项目统一的浅色表面、边框、文字和主题色资源，保持与现有自定义参数下拉框一致。
- 控件使用轻量圆角、细边框和清晰的悬停/聚焦状态；不得使用突兀的原生系统边框、默认渐变或默认阴影。
- 下拉箭头必须位于控件右侧并在控件垂直方向居中；展开和收起时箭头应围绕自身中心旋转，不得发生位置跳动或上下偏移。
- 展开状态需要有明确的主题色边框或焦点反馈；选中项需要有统一的高亮背景和文字颜色。
- 下拉菜单应与触发控件保持同一视觉体系，使用统一的边框、圆角、间距、阴影和选中态。

### 宽度与布局

- 下拉框宽度不得机械写死为某一个项目级固定值；应根据字段内容、标签长度、所在容器和当前视口自适应计算。
- 同一组横向排列的下拉框，应在满足内容可读性的前提下保持列宽、间距和基线对齐；不得因为某个标签换行导致同组控件上下错位。
- 弹窗或表单容器应根据下拉控件组的实际内容收紧宽度，避免右侧或底部出现无意义的大块留白。
- 在窄窗口、窗口缩放和高 DPI 场景下，控件必须允许收缩或换行，但不得溢出容器、遮挡按钮或造成横向滚动条。
- 菜单宽度默认跟随触发控件；如果选项文本更长，应采用合理的最小宽度、内边距或文本省略策略，确保选项可读且不破坏布局。

### 交互要求

- 点击触发控件后展开菜单，再次点击或选择选项后收起；同一时间只允许一个下拉菜单保持展开。
- 点击菜单外部、关闭弹窗或切换页面时，必须关闭已展开菜单并清理相关状态。
- 键盘、鼠标和触摸操作应保持一致的选中状态反馈；展开、悬停、选中和禁用状态必须可区分。
- 下拉选项变更应通过绑定、命令或统一交互逻辑同步到 ViewModel，不得在页面中复制业务处理代码。
- 大量选项时应考虑滚动、搜索、分组或虚拟化，避免一次性渲染造成明显卡顿。

### 复用与实现要求

- 新增下拉控件前，必须先检查 `Control` 和 `Resource/Style` 中是否已有统一实现；优先复用现有控件和样式。
- 如果现有控件基本满足需求，应通过可选属性、模板参数或扩展样式适配，不得复制一套近似控件。
- Web 原型中的自定义下拉（如 `.param-select`、`.param-select-trigger`、`.param-select-menu`）属于项目统一参考样式；后续原型页面新增下拉应复用其结构和状态表现。
- WPF 实现应优先基于 HandyControl 的下拉控件和项目公共样式扩展，保持与 Web 原型相同的圆角、边框、箭头居中、展开高亮和自适应宽度原则。
- 统一样式的修改必须评估对现有页面的影响；如存在兼容风险，应新增扩展样式或可选配置，不能直接破坏已有页面。

## 投屏卡片与窗口自适应强制规范

设备投屏页面需要同时适配窗口缩放、主控打开/关闭、屏数变化、高 DPI 和不同显示器尺寸。后续实现 WPF/C# 页面时，禁止仅按某一个设计稿尺寸写死布局，也禁止让手机卡片在可用空间不足时被无限压缩。

### 自适应布局要求

- 手机卡片布局必须以投屏列表容器的实际可用宽度为依据，不得只根据整个窗口宽度或固定设计稿宽度计算。
- 顶部设备标题栏、手机画面、底部任务提示区、字体、内边距和卡片间距必须随单张卡片的实际宽度联动调整。
- 禁止为标题栏、任务提示区和卡片间距设置只能适用于单一窗口尺寸的固定值；允许设置经过验证的最小值、最大值和默认值，用于保证可读性与视觉稳定性。
- 设备编号、型号、任务名称、状态和进度必须限制在各自区域内；空间不足时使用合理的文本省略、隐藏次要信息或换行策略，不得与相邻卡片重叠。
- 同一行手机卡片的标题栏和任务提示区应保持视觉对齐，动态内容不得造成单张卡片高度异常并挤乱整行布局。
- 手机卡片之间的水平和垂直间距必须根据容器宽度在合理范围内缩放，禁止在窄窗口下仍保持过大的固定间距。
- 卡片必须设置最小可读宽度。屏数较多且空间不足时，应优先让设备列表自身滚动或采用明确的降级布局，禁止把卡片压缩到标题、画面或任务信息无法辨认。
- 页面本身不应因设备卡片数量或屏数变化产生横向溢出；如必须横向滚动，只允许滚动发生在设备列表容器内部。
- 主控关闭后，设备列表必须释放主控区和控制栏占用的空间并自动扩展为全宽；重新打开主控后再恢复对应布局，不得保留无意义空列。
- 列表视图与屏幕视图切换时，任务详情、选中状态、屏数和主控状态不得因自适应布局而丢失。

## 设备状态色与任务类型色规范

设备投屏列表、设备选择器、任务列表和主控详情中的颜色必须区分“设备/任务状态”和“任务类型/平台分类”。禁止让同一个状态圆点在不同页面因为任务类型不同而变成另一种状态色，避免 100 台设备场景下产生误判。

### 状态颜色统一语义

- 绿色（`--green`）：空闲、可接受新任务、已验证且无需人工处理。
- 蓝色（`--blue`）：任务正在运行或设备正在执行自动化操作。
- 橙色（`--orange`）：等待人工处理、人工接管、等待验证或前置条件未完成。
- 红色（`--red`）：异常、失败、登录验证失败或需要立即关注的错误。
- 灰色/中性色：离线、未连接、未知或尚未获取状态；不得用绿色代替未知状态。

### 使用要求

- 投屏手机卡片右上角状态圆点、设备状态徽章、设备选择器状态列必须只表达设备/任务状态，统一遵循上述颜色映射。
- 任务进度条颜色必须跟随任务状态；运行中使用蓝色，等待人工使用橙色，失败使用红色，完成或空闲使用绿色。进度百分比不能单独改变状态语义。
- 任务类型、平台、账号分组和运营动作不得复用状态圆点颜色表达。需要区分“发布、评论审核、私信分流、代理检测”等类型时，优先使用文字、图标、平台标识或独立的类型标签。
- 紫色、青绿色等辅助色可以用于 AI、平台、任务类型或分组标识，但不得在状态圆点、状态徽章中表示“运行中/空闲/异常”等设备状态。
- 同一设备在投屏卡片、设备列表、任务中心、AI 执行队列和主控详情中的状态颜色必须保持一致；颜色来源应绑定统一的状态字段或状态转换器，不得按页面自行判断。
- 状态颜色必须同时满足明亮主题、暗黑主题和高 DPI 对比度要求；不能只依赖颜色传达关键信息，必须同时显示状态文字、图标或可访问名称。

### WPF 实现要求

- 优先使用 WPF 的 `Grid`、`ItemsControl`、支持虚拟化的列表控件、可复用 Panel、`Viewbox`、`MinWidth` / `MaxWidth`、样式触发器和 `VisualStateManager` 实现响应式布局。
- 实现前必须检查 HandyControl 是否已有可复用的 `Slider`、列表、滚动、弹窗或布局样式；屏数控制条优先参考或复用 HandyControl `Slider`，不得直接使用未经统一样式处理的默认 WPF 控件外观。
- 屏数属于界面状态，应绑定到 ViewModel 的可观察属性；屏数调整命令和状态同步遵循 CommunityToolkit.Mvvm，不得在 code-behind 中保存业务状态。
- 容器宽度、卡片尺寸和视觉状态切换属于纯界面行为，应优先通过 XAML、绑定、样式、触发器、转换器、附加属性或可复用 Behavior 实现。
- 如果 WPF 内置布局无法满足卡片自适应，新增能力应封装到 `Control` 中的可复用控件、Panel 或 Behavior，并将通用样式放入 `Resource/Style`；禁止在页面 code-behind 中堆积尺寸计算。
- 不得在 `SizeChanged` 中直接高频重建全部设备集合、重新创建全部投屏对象或重复启动视频流。确需响应尺寸变化时，必须节流或合并更新，并只调整必要的布局状态。
- 约 100 台手机同时显示时必须启用或评估 UI 虚拟化、投屏分级刷新和非焦点设备低帧率策略，避免窗口缩放引起 CPU、GPU 和内存占用突增。
- 卡片尺寸变化不得重新连接设备、重启投屏流或重置任务状态；视觉尺寸与设备会话生命周期必须解耦。

### 强制验证矩阵

新增或修改投屏页面后，至少验证以下组合：

- 窗口宽度：`1024px`、`1280px`、`1440px`、`1920px` 或对应 WPF 逻辑像素宽度。
- 屏数：最小值 `5`、常用值、最大值 `20`。
- 主控状态：主控打开、主控关闭、重新选择主控。
- 视图状态：屏幕视图、任务列表视图。
- 主题与显示：明亮主题、暗黑主题、常用 DPI 缩放比例。

每组验证必须检查：标题栏是否可读、手机间距是否合理、任务提示区是否完整、选中边框是否清晰、设备列表是否只在必要时内部滚动、页面是否存在整体横向溢出、任务与主控状态是否保持。禁止只在单一开发机窗口尺寸下验证后交付。

## 列表滚动条视觉强制规范

项目内所有列表型和可滚动区域默认不得显示系统原生滚动条，包括但不限于设备投屏列表、任务列表、AI 对话列表、实时队列、设备选择器、分组列表、表格、树形列表、弹窗内容区和下拉菜单。隐藏滚动条仅指隐藏视觉外观，必须保留完整滚动能力和内容可访问性。

### 基础要求

- 禁止通过 `overflow:hidden`、裁剪内容、禁用 `ScrollViewer` 或限制集合高度的方式伪装成“没有滚动条”。
- 隐藏滚动条后仍必须支持鼠标滚轮、触控板、触摸滚动、键盘方向键、`PageUp` / `PageDown`、`Home` / `End` 和程序化定位（适用时）。
- 横向滚动确有必要时，同样隐藏横向滚动条视觉，但必须保留触控板、Shift + 滚轮、触摸拖动或明确的替代导航方式。
- 内容到达边界时不得被遮挡；列表底部、右侧和最后一个条目必须能够完整滚动到可视区域。
- 隐藏滚动条不得影响列表虚拟化、增量加载、选中项定位、键盘焦点、自动滚动和无障碍访问。
- 明亮主题、暗黑主题和高 DPI 下均不得重新出现系统默认滚动条、轨道或滚动按钮。

## 全页面父容器自适应规范

除设备投屏外，运营总览、任务中心、AI 调度助手、账号与内容、互动工作台、代理与环境、设计系统和系统设置等页面，也必须始终跟随 `Window` / `shell` / 页面内容父容器的实际可用空间布局。禁止因为设计稿或开发机尺寸而给普通页面设置固定画布宽度、固定右侧空白或只能在单一分辨率下成立的分栏。

### 布局要求

- 页面根容器、内容区和主要工作区必须设置合理的 `MinWidth=0`，允许 Grid 列、ItemsControl、卡片和表单在父容器中收缩或重排；不得让子控件的默认最小宽度把整页撑出横向溢出。
- 宽窗口下页面内容应占用父容器的可用宽度；不得通过固定 `MaxWidth` 制造大块无意义留白。确需限制阅读宽度的文本区域，应只限制文本区域，不得限制页面主工作区。
- 多列布局必须使用 `Grid` 的 `*`、`MinMax(0,*)`、自适应列或可复用响应式 Panel；窗口变窄时按优先级降为两列、单列或内部滚动，不得将固定列宽硬塞进窄窗口。
- 标题栏、工具栏、按钮组和筛选区必须允许换行、收缩或内部滚动；文字、按钮和下拉不得互相遮挡，也不得依赖页面级横向滚动才能操作。
- 卡片、表格、时间线、对话列表、资源面板和详情面板必须检查父容器高度变化。固定高度只允许用于明确的设备画面或编辑器区域，并应配合 `MinHeight` / `MaxHeight`、内部滚动或 `*` 行伸展。
- 日历、宽表格等确实需要保留最小列宽的内容，只允许在自身内容容器内滚动；页面根容器不得出现整体横向滚动条。
- 需要隐藏次要栏时，必须释放其占用的 Grid 列并让主内容重新计算宽度；禁止只设置 `Visibility=Hidden` 后留下空列。
- 任何响应式规则都必须同时验证窗口放大、缩小、侧栏折叠、明暗主题和高 DPI；页面状态、选中项、任务进度和输入内容不得因尺寸变化丢失。

### Web 原型与 WPF 验证要求

- Web 原型应优先使用 `width:100%`、`min-width:0`、`minmax(0,1fr)`、`clamp()`、容器查询和经过验证的断点；禁止为每个页面复制一套互相冲突的固定尺寸规则。
- WPF 页面应优先使用 `Grid`、`ItemsControl`、`ScrollViewer`、`MinWidth` / `MaxWidth`、样式触发器和 `VisualStateManager`，并将通用响应式样式放入 `Resource/Style`。
- 页面验证至少覆盖宽窗口、常用工作窗口和窄窗口，例如 `1920px`、`1440px`、`1024px`、`768px` 或对应 WPF 逻辑像素；每个页面都要检查是否存在页面级横向溢出、右侧无意义留白、内容遮挡、卡片错位和底部内容不可达。
- 响应窗口尺寸时只更新布局状态，不得重建全部业务集合、重新连接设备、重启投屏流或重置任务；UI 尺寸与业务状态、设备会话生命周期必须解耦。

### WPF 实现要求

- WPF 中应通过公共 `Style`、控件模板或 HandyControl 可复用样式统一隐藏滚动条视觉，相关资源统一放入 `Resource/Style`；禁止在每个页面重复复制 ScrollBar 模板。
- `ListBox`、`ListView`、`DataGrid`、`TreeView`、带滚动的 `ItemsControl`、下拉列表和自定义设备列表必须复用同一套无可见滚动条规范。
- 可使用 `ScrollViewer.VerticalScrollBarVisibility="Hidden"` 和 `ScrollViewer.HorizontalScrollBarVisibility="Hidden"` 隐藏视觉，但不得使用会禁用所需滚动方向的配置。
- 如果 HandyControl 已提供合适的 ScrollViewer、列表或 ScrollBar 样式，必须优先复用或扩展其样式，并保持项目明暗主题一致。
- 滚动状态属于 View 行为；业务 ViewModel 不应持有 ScrollViewer、ScrollBar 或具体视觉元素引用。需要定位选中项时应通过 Behavior、附加属性或清晰的视图服务实现，禁止在 ViewModel 直接操作控件。
- 约 100 台设备的列表必须继续启用或评估虚拟化。隐藏滚动条不得通过替换为不支持虚拟化的外层 `ScrollViewer` 实现，否则会导致全部项目一次性创建。

### Web 原型要求

- Web 原型统一通过公共滚动条样式隐藏 Chromium/WebKit、Firefox 和旧版 Microsoft 浏览器的滚动条视觉。
- 可滚动容器仍使用 `overflow:auto` 或对应滚动方向配置，不得改为 `overflow:hidden`。
- 后续新增列表、弹窗、下拉菜单或对话区域时应自动继承公共隐藏样式，不得新增可见的原生滚动条。

### 强制验证点

- 使用足以产生溢出的数据量验证设备列表、任务列表、对话列表、弹窗和下拉菜单。
- 确认页面和列表中没有可见的竖向或横向原生滚动条。
- 分别使用鼠标滚轮、触控板或模拟滚动、键盘导航验证内容仍可到达首项和末项。
- 验证滚动后选中状态、任务进度、主控状态和已加载数据不丢失。
- 验证明亮主题、暗黑主题、窗口缩放和高 DPI 场景不会重新显示滚动条。

## 原型预览与浏览器使用规范

- 修改或更新 Web 原型后，禁止自动在 Codex 内置浏览器中打开、刷新或展示页面。
- 用户统一使用 Chrome 浏览器自行审核原型；Codex 只需确保本地原型服务正常，并提供可访问的本地地址。
- 除非用户明确要求，Codex 不得主动打开、控制或刷新 Chrome。
- 原型服务必须使用独立后台进程运行，不得依赖会随当前终端会话结束的前台进程。
- 每次重新架设原型服务后，必须检查监听端口和原型页面的 HTTP 状态，确认服务可访问后再交付地址。
- Web 原型包含多个内联 `<script>` 时，修改后必须逐个提取并执行 JavaScript 语法检查；任一脚本块解析失败都视为未完成，不得交付。

## 产品平台与运营能力范围

- 产品正式支持五个社交媒体平台：Facebook、YouTube、Instagram、TikTok、Twitter/X（界面可显示为 X / Twitter）。后续页面、任务、数据模型和需求文档不得遗漏或擅自替换这五个平台。
- 产品采用“AI 调度 + 规则化自动化脚本”双引擎模式：AI 负责理解自然语言、生成执行计划、选择脚本和处理低置信度分支；经过审核的固定脚本负责稳定、可复现、可暂停的设备操作。任何一侧都不能替代另一侧。
- 五个平台的基础运营能力均需纳入规划：账号接入/登录、注册或人工开户注册流程、登录状态与安全验证、养号、发布作品、运营数据采集。
- 养号脚本必须区分两种入口：推荐页养号、关键词搜索结果养号。动作可按策略随机组合浏览视频/帖子、关注、点赞、转发、收藏和评论，并配置频率、停留、并发、人工审核与平台安全限制。
- 发布作品必须覆盖视频和图文两种内容形态，并支持素材审核、文案/标签、发布计划、批量设备执行、失败重试和结果记录；具体平台能力差异必须通过平台适配层表达，不得在页面中硬编码成一套流程。
- 标题/文案与标签/关键词必须使用两个独立的电脑本地 TXT 文件作为来源，按非空行抽取，分别记录文件路径与读取行数；不得用同一个文本框或同一个 TXT 同时承担两类内容。
- 发布素材必须支持电脑本地目录、分发规则（随机抽取/按时间抽取/按顺序抽取）和抽取数量（默认 1），运行时将标题、标签和素材按设备任务实例分配。
- 跨平台发布目标可多选并可配置平台顺序与间隔；执行方式仅允许“插队执行（立即执行）”或“顺序执行（等待上一个任务执行完毕）”。平台失败默认自动跳过，可选有限次数重试后跳过，不得以“等待人工处理”阻塞跨平台自动化队列。
- 运营数据至少包括作品播放/观看量、点赞量、评论量、转发量、收藏量，以及账号粉丝数、关注数、作品数、互动率、最近更新时间和异常状态。
- 任务的直接操作对象始终是 Android 手机，可按设备单选或多选、按设备分组批量选择；账号、平台和素材是手机上的运行上下文，不得把账号误作任务目标。
- AI 与固定脚本创建的任务必须进入同一任务队列，显示平台、运营动作、脚本/计划名称、目标手机数量、当前步骤、进度、状态和可人工接管节点。

## 安卓设备执行基础架构

- 本产品的实际执行基础是约 100 台 Android 手机，手机通过 ADB 建立设备连接，通过 scrcpy 提供投屏与主控画面，并由安卓自动化脚本在手机端执行登录、浏览、互动、发布和数据采集动作。
- 任务的直接执行对象始终是 Android 手机；平台账号、素材、代理、脚本和运行参数都是该手机上的执行上下文。不得把浏览器账号或账号记录误当作任务执行对象。
- 设备任务必须考虑 ADB 设备序列号、在线状态、授权状态、应用包名、前台页面、代理环境、屏幕方向、文件传输、剪贴板/输入法、截图与日志回传等前置条件。
- 发布作品不能只保存“平台 + 文案”。必须明确素材来源和进入手机的方式：项目素材库选择、手机相册/本地文件、通过 ADB 推送到手机、通过手机网络下载 URL、对象存储 URL 拉取、或手机拍摄后上传；每种来源都要有路径/地址、文件类型、大小/时长、哈希或完整性校验和失败回退策略。素材完整性检查属于系统内部前置步骤，不作为用户额外确认项。
- scrcpy 只负责投屏、画面查看和人工接管，不承载业务状态；调整投屏尺寸、帧率或主控状态不得重启 ADB 会话、重置任务进度或重新创建自动化实例。
- ADB、scrcpy 和自动化脚本调用必须在 Service/适配层封装，ViewModel 只编排任务状态和命令；不得在 View 或页面中直接拼接 adb 命令、操作 scrcpy 进程或保存设备句柄。
- 每台手机的任务应记录连接检查、代理检查、应用启动、素材传输、脚本步骤、人工接管、结果校验和清理释放等阶段；失败时保留设备序列号、步骤、返回信息和日志索引。
- 约 100 台设备执行时必须限制并发、控制投屏刷新和文件传输带宽，避免批量任务同时启动导致 CPU、GPU、内存、ADB 通道或网络出口过载。
